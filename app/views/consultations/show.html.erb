<div class="show-page">
  <div class="doc-show">
    <section class="about-doc">
      <div class="about-doc-wrapper">
        <div class="about-doc-inner">
          <div class="about-doc-details">
            <div class="about-doc-details-jobs mt-0">
              <h1>Resumo</h1>
            </div>
            <div>
            <h2>Consulta dia <%= @consultation.start_time.day %>/<%= @consultation.start_time.month %>/<%= @consultation.start_time.year %> as  <%= @consultation.start_time.hour %>:<%= @consultation.start_time.min %> de <%= @consultation.patient.first_name %></h2>
            </div>

            <div class="about-doc-details-jobs">
              <h3>Dr. <%= @consultation.doctor.user.first_name %> <%= @consultation.doctor.user.last_name %> </h3>
              <p><%= @consultation.doctor.description %></p>
            </div>
            <div class="about-doc-details-academic">
              <h3>Endereço</h3>
              <p><%= @consultation.doctor.address %></p>
            </div>
          </div>
            <div id="map"
              style="width: 300px; height: 300px; border-radius: 4px"
              data-markers="<%= @markers.to_json %>"
              data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
            </div>

          </div>
        </div>

    </section>
    </div>
  </div>
</div>

